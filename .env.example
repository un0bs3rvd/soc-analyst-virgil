# Copy to .env and fill in what you have, or export these in your shell.
# With none of them set, the demo runs end-to-end in clearly-labeled mock mode.

# Jev decision engine (early access: https://typesafe.ai)
TYPESAFE_API_KEY=

# Investigator — pick ONE provider (INVESTIGATOR_PROVIDER in the notebook config cell):
GEMINI_API_KEY=        # https://aistudio.google.com/apikey
OPENAI_API_KEY=        # or MINIMAX_API_KEY — any OpenAI-compatible endpoint
ANTHROPIC_API_KEY=     # https://console.anthropic.com
